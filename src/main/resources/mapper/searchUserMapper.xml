<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carpe.diem.mapper.SearchUserMapper">
    <select id="searchUserMapper" resultType="com.carpe.diem.entity.User">
        select * from user

    </select>

    <delete id="truncateUser">
        truncate table user;
        truncate table test;
    </delete>

</mapper>

